# 策略进化 Playbook

> 从 `EVOLUTION.md` 的复盘记录中提炼的**可执行规则改进**。
> **待批准区的规则在用户签字前一律不生效**，AI 不得据此交易。

---

## 这个文件的角色

```
EVOLUTION.md（每笔亏损的归因与教训）
        ↓ 同类归因累计 ≥3 次
PLAYBOOK.md（本文件，提炼成规则提案）
        ↓ 用户批准
AGENT_TRADING_RULES.md（正式生效，版本 +1）
```

- `EVOLUTION.md` 是**流水**：每一笔亏损的具体复盘，只追加
- `PLAYBOOK.md` 是**提案区**：把重复的教训固化成规则，等批准
- 章程是**生效规则**：只有批准后才写入

**为什么需要阈值 3 次**：用 1 个样本改规则是过拟合，会把噪音当成规律。累计 3 次说明是系统性缺陷而非运气。

---

## ⚠ 重大变更：章程 v2.0 效力分层（2026-09-02，用户直接指令）

**本文件的角色已变。** 用户 2026-09-02 指示：「章程松绑，应该由AI根据实际情况决策，章程应该只有引导作用，不起决定性限制。」

章程 v2.0 据此把全文划为两层，**提案流程随之分级**（章程 §11.4）：

| 改动对象 | 谁可决定 | 是否需要走本文件 |
|---|---|---|
| **L2 引导基准**（开仓阈值 / 指标数值 / 档位 / 路径 / 时限 / 频次 / 消息面闸门） | **AI 可自主调整** | ❌ 不需要。直接改章程 + §13 记录 + 当轮报告说明。依据须来自 §0.4 绩效数据或 ≥3 次同类归因，**不得凭单次直觉** |
| **L1 硬约束**（章程 §1 的 10 条） | **仅用户** | ✅ 必须先入本文件待批准区静置并上报 |
| **归档格式 / 降级通道 / 流程步骤** | **仅用户** | ✅ 同上（属 L1-7 审计范畴） |

> 即：本文件从「**所有章程改动的必经关卡**」收窄为「**L1 与流程/归档改动的关卡**」。
> L2 数值调整改由章程 §13 变更记录 + §0.4 绩效数据直接承载，不再走提案静置。
> **但「不得凭单次样本改规则」（§11.5）依然有效** —— 松绑的是审批流程，不是统计纪律。

---

## 当前状态

**待批准提案 0 项。** 提案 P003/P004 已**作废**（评审驳回 + 用户批准作废），P005/P006 已批准并写入章程 v1.5。
**章程已升至 v2.0**（2026-09-02，用户直接指令落地，非提案流程 —— 详见下方「用户直接指令落地记录」）。

| 编号 | 提案 | 状态 | 生效位置 | 是否改动 §4 门槛 |
|---|---|---|---|---|
| P001 | 「下一轮触发价位」必须与 §4 成文门槛同源 | ✅ `approved` 2026-09-02 | 章程 **§7.1** | ❌ 不改动 |
| P002 | MCP 降级通道 `scripts/mcp_call.py` 补充备案 | ✅ `approved` 2026-09-02 | 章程 **§2.1** | ❌ 不改动 |
| P003 | 下单接口禁止传 `clOrdId` | ❌ `rejected` 2026-09-02（评审驳回，用户准其作废） | — | ❌ 不改动 |
| P004 | 修订 §2.1：降级通道不支持写操作 | ❌ `rejected` 2026-09-02（前提证伪，用户准其作废） | — | ❌ 不改动 |
| P005 | 启用 `clOrdId` 订单幂等保护（P003 修正后的正向产出） | ✅ `approved` 2026-09-02 | 章程 **§6.1** | ❌ 不改动 |
| P006 | `--list-tools` 需提示隐藏的写工具与数量 | ✅ `approved` 2026-09-02 | 章程 **§2.1.1** + 脚本 `mcp_call.py` | ❌ 不改动 |

> **✅ 2026-09-02 结案（运维评审 → 用户回复「准」）**
> - **P003 / P004 已否决**：二者现象真实但归因错误。P003 把 `clOrdId` 的**字符格式错误**记为「参数不可用」；
>   P004 把**默认 `--read-only` 启动**记为「服务端不支持写」（实测加 `--allow-write` 后 48 → **78** 工具，
>   下单撤单均成功）。根因详见下方「已否决的提案」。
> - **P005 / P006 已批准并落地**：章程升至 **v1.5**，新增 §6.1 订单幂等保护与 §2.1.1 降级通道写能力澄清；
>   新增脚本 `scripts/order_id.py`；改动 `scripts/mcp_call.py`。
> - 全过程**未触碰 §1 四条红线、未改动 §4 任何开仓阈值**。
> - 流程顺序合规：评审复现证伪 → 导出正向提案 → 静置待批准区上报 → 用户批准 → 才写章程。

> **2026-09-02 用户回复「提案通过」，P001/P002 已同时批准并写入章程 v1.4。**
> 二者均不触碰 §1 四条红线、不改动 §4 任何开仓阈值。详见下文「已生效的规则变更」与章程 §13。

归因统计（1 笔复盘，均未达阈值 3）：

| 归因类别 | 次数 | 距提案还差 |
|----------|------|-----------|
| `stop_loss` | 1 | 2 次 |

> 说明：P001~P006 全部来源于**日报复盘与运维测试**发现的规范性 / 工程缺陷
> （书写规范、架构备案、执行层幂等防护、能力边界可观测性），
> 不属于 §11.3 的七类交易归因，因此不受「同类累计 ≥3 次」阈值约束。
> 六者均**不放宽任何开仓门槛、不触碰 §1 四条红线**。

---

## 用户直接指令落地记录 — 章程 v2.0「松绑」（2026-09-02）

> **性质说明**：本条**不是提案**，无需批准 —— 它是用户明确指令的执行记录。
> 之所以仍写入本文件，是因为 L1-7（归档只追加）要求任何章程结构性变动都必须可追溯。

**用户指令原文**：「章程松绑，应该由AI根据实际情况决策，章程应该只有引导作用，不起决定性限制。」

**触发背景（可复核的实证依据）**

R000004（2026-09-02 11:12）出现两个现象，共同证明机械阈值体系存在结构性缺陷：

1. **ETH 路径 A 七项过六项被单项否决** —— 共振分 −33.6 首次越过 28、分位 27.98% 脱离禁区、RR 2.04、funding 0.01%、无同向持仓、趋势不冲突，**唯量比一项不达标**（15m 0.327 / 1H 0.704 / 4H 0.640）。
2. **路径 B 两条要求数学上互斥** —— 「分位 ≤25%」与「RR ≥1.5」交集为空：分位恰为 25% 时 BTC RR 仅 **1.10**、ETH 仅 **1.08**，须回落至约 **17%** 分位 RR 才达标。根因是分位取 60 根 K 线的线性刻度，而止盈（4H 布林中轨）只反映最近 20 根，量纲不一致。

第 2 项已写入章程 **§4.4**（原计划按 §11.5「单样本不改规则」仅作观察记录，现因用户指令直接入章程作为松绑依据）。

**落地内容（章程 v2.0，共 9 项，详见章程 §13）**

- 新增 §0.2 效力分层（L1 不可裁量 / L2 可裁量），判定四步，**正反向偏离举证责任对称**
- 新增 §0.3 裁量偏离留痕（五必填字段，核心是 `falsifier` 可证伪预判）
- 新增 §0.4 裁量绩效独立核算 + 新脚本 `scripts/deviation_stats.py`
- §1 由「四条红线」扩为 L1 完整清单 10 条，每条附不可裁量理由
- §4 全节由「门槛」改为「先验基准」，**数值一律未动**（保留为 §0.4 对照基线）
- §4.2 数据缺失不再一律观望；§7 复盘债硬拦截改强建议；§5.3 档位改建议起点
- §11.4 提案权分级（L2 归 AI，L1/流程/归档归用户）
- L1-5 单笔风险硬顶 2.0% → **2.5%**；L1-6 月度回撤 8% 降档 → **12% 强制停止**并升格 L1
- 顺带修正 §2 过时的实盘仓位数据、§10.6 事件日历

**AI 的主动保留（须用户知晓，可随时要求删除）**

用户指令字面可理解为"全部条文皆可裁量"。我保留了 **10 条 L1 未松绑**，理由是这些条文的失败模式**不是"这笔亏了"，而是"没有下一笔了"**：

| 保留项 | 若松绑的后果 |
|---|---|
| L1-3 实盘只读 | AI 可动用用户真实资金 —— 这是授权问题，不是策略问题 |
| L1-4 止损必须存在 | 单笔即可归零账户（可裁量的是距离/形式，已明确） |
| L1-5 风险硬顶 2.5% | 一次"高确信"重注即造成不可恢复回撤 |
| L1-6 回撤 12% 熔断 | 无终止条件的连续下注数学上必然走向归零 |
| L1-7 归档只追加 + 偏离必留痕 | 可修改历史 → §0.4 核算与 §11 进化闭环全部沦为表演 |
| L1-10 禁亏损加仓 | 马丁格尔结构导致无界亏损 |

**这是 AI 的判断，不是用户指令。** 若用户认为其中任意一条也应松绑，明示即可执行并记入 §13。

**这次松绑为什么不会退化成无纪律下注**

关键不在保留了几条 L1，而在 §0.3 + §0.4 的组合：偏离必须写下**可证伪预判**，且裁量单绩效**与基准单分开统计**。
若 AI 的判断确实优于机械阈值，数据会证明并把成功裁量固化为新基准；若不优于，数据同样会证明并触发收紧提案。
**自由度由绩效持续赎买，两种结果都比凭立场争论更有价值。**

---

## 已生效区（2026-09-02 用户批准，已写入章程 v1.4 与 v1.5）

### 提案 P001 — 「下一轮触发价位」必须与 §4 成文门槛同源

- **状态**：✅ `approved` — 2026-09-02 用户批准，**已写入章程 §7.1，即刻生效**
- **提出时间**：2026-09-01 23:5x CST
- **来源**：`reports/daily/2026-09-01.md` 当日复盘（**非** `review_trade.py` 自动生成的归因提案，属**规范性缺陷**，不受"同类归因 ≥3 次"阈值约束）
- **性质**：约束 AI 自身的书写规范，**不改动 §1 四条红线、不改动 §4 任何阈值、不放宽任何门槛**

**问题事实（可复核）**

R000002 在轮次日志中手写预设「BTC 回踩 77,400 买入（长下影 + 15m 量比≥1.2），止损 77,000，止盈 78,556」。当日 21:00 该形态**真实成立**：最低 77,400.90 精准触 4H 布林下轨 77,400.52，伴当日最大 1H 成交量 785,302，收 78,038.00 形成 +0.82% 长下影锤线。

但该 setup **拿去过章程门槛会被否**：

| 路径 | 关键条件 | 当时实测 | 判定 |
|---|---|---|---|
| 路径 B（§4.3 做多） | 4H 区间分位 ≤ 25% | **31.43%**〔(77,400.90−75,513.2)÷(81,520.0−75,513.2)〕 | ❌ 不通过 |
| 路径 A（§4.1） | \|共振分\| ≥ 28 | 该低点比 21:31 实测 −10.4 更负，仍远低于 28；且负分为做空倾向，与做多相反 | ❌ 不通过 |

**即系统给自己下达了一份规则不支持的作业。** 当日未执行（3 轮采样均在形态回补之后），因此**不构成违规**，但属明确的规范性缺陷 —— 若某轮恰好采样到该时点，AI 将面临「照自己写的预设做」与「照章程门槛做」的冲突。

**提案内容**

在 §7 第 6 步（决策执行）或 §8 归档规范中增补一条书写约束：

> 轮次日志／决策全文中预设的每一个「下一轮触发价位」，必须显式标注：
> ① 该触发走**路径 A 还是路径 B**；
> ② 触发时**各条门槛的预计取值**（至少覆盖路径 A 的 ①②③⑤ 或路径 B 的全部触发条件）。
> **凡在书写当场即可判定过不了门槛的价位，一律不得写入预设清单。**
> 预设价位仅为盯盘提示，**任何情况下不得替代 §4 的逐条校验**。

**预期收益**：消除"预设清单"与"成文门槛"两套并行标准，杜绝未来某轮因预设诱导而降门槛开仓（§0.1 铁律 2 的最大风险敞口）。

**副作用评估**：预设清单会变短（因部分位置本就不合规），可能让用户观感上"机会更少"。但这是把隐藏的不一致显性化，不是真的减少机会。

---

### 提案 P002 — MCP 降级通道 `scripts/mcp_call.py` 补充备案

- **状态**：✅ `approved` — 2026-09-02 用户批准，**已写入章程 §2.1，即刻生效**
- **提出时间**：2026-09-01 23:5x CST
- **来源**：`reports/daily/2026-09-01.md` 纪律执行核查

**事实**：R000003 因 `mcp__okx-demo__*` / `mcp__okx-live__*` / `mcp__mx-ds-mcp__*` 三个连接器均未暴露为工具，AI 新建 `scripts/mcp_call.py`（JSON-RPC over stdio 直连）恢复只读能力，避免本轮因数据缺失空转。

**合规性评估**：动作本身符合 §4.2（"数据获取失败 → 观望，不允许凭记忆或猜测下单"—— 降级通道恰好避免了数据缺失），且已内置三重安全措施：服务端 `--read-only`、写工具白名单二次校验、`--profile live` 强制拒写。**未违反任何红线。**

**但**：在交易链路上新增一条数据通道属**架构变更**，理应经用户备案确认，而非由 AI 单方面落地后仅记录于轮次日志。

**提案内容**：请用户确认是否将 `scripts/mcp_call.py` 作为**正式降级通道**写入章程 §2（运行环境）与 §8（归档规范），并明确其安全边界（live 永久只读、写操作需显式 `--allow-write`）。若用户否决，则该脚本仅作临时应急，每次使用须在日志中单独声明。

---

### 提案 P005 — 启用 `clOrdId` 订单幂等保护

- **状态**：✅ `approved` — 2026-09-02 用户回复「准」，**已写入章程 §6.1，即刻生效**
- **来源**：2026-09-02 运维评审（由 P003 归因修正导出的正向提案）
- **是否改动 §4 门槛**：❌ 不改动

**要解决的问题**：下单请求超时时，无法区分「交易所未受理」与「已受理但响应丢失」。
无幂等 ID 只能在「重试 → **重复建仓**」与「不重试 → **漏单**」之间二选一，两者都直接违反 §5 仓位纪律。
这是执行层**唯一没有防护**的故障模式。

**落地内容**（章程 §6.1）

- 每次 `swap_place_order` / `swap_place_algo_order` **必须**附带合规 `clOrdId`
- 格式 `^[A-Za-z][A-Za-z0-9]{0,31}$` —— 字母开头、仅字母数字、≤32 字符；**禁止 `_` `-`**
- 必须用 `scripts/order_id.py` 生成并登记，**禁止 AI 手写**（手写天然会用 `_`，而这是唯一已知失败模式）
- 超时重试必须用**同一 ID**；事后按 `clOrdId` 回查成交笔数确认未重复建仓
- 追踪以 `ordId` 为主键，`clOrdId` 用于幂等保护与对账

**配套脚本**：`scripts/order_id.py`（2026-09-02 新建，实测通过）

| 命令 | 用途 |
|---|---|
| `--round R000004 --seq 1 --params '{...}'` | 生成 ID 并登记进 `state/order_idem_<round_id>.json` |
| `--verify <id>` | 格式自检，手写 ID 前必做 |
| `--round R000004 --list` | 本轮已登记 ID，事后对账 |

实测：`okxr4n1t283145` ✅ 合规；`smoketest_open_01` ❌ 精确报「含非法字符 `_`」；`omt-1a` ❌；`1abc` ❌「首字符必须是字母」。同 `--round`+`--seq` 重复生成被拒绝（防手滑重复下单）。

---

### 提案 P006 — `--list-tools` 需提示被隐藏的写工具

- **状态**：✅ `approved` — 2026-09-02 用户回复「准」，**已写入章程 §2.1.1 并改动 `scripts/mcp_call.py`，即刻生效**
- **来源**：2026-09-02 运维评审（由 P004 归因修正导出的正向提案）
- **是否改动 §4 门槛**：❌ 不改动

**要解决的问题**：`--list-tools` 在默认只读模式下只输出 48 个工具，**静默隐藏**其余 30 个写工具且不给任何说明。
P004 的误判正源于此 —— 把「默认配置」当成了「能力缺失」。不可观测的边界，等同于不存在的边界。

**落地内容**

1. 章程 §2.1.1：明确**读与写均可降级**，写加 `--allow-write` 即可（48→78 工具），
   并写明「禁止仅凭默认模式的工具列表推断能力边界」
2. `scripts/mcp_call.py --list-tools` 输出被隐藏写工具的**数量与名称**
3. 新增 🔴 降级状态告警：走降级通道且存在需平仓/补止损的持仓时，当轮邮件升级为高优先级告警
   —— 理由是**须告知用户系统处于降级状态**，而非原文所述「无法执行风控动作」（实测能执行）
4. 新增 `--no-probe` 开关，可跳过探测以省一次子进程启动

**工程取舍**：提示输出到 **stderr**，`stdout` 保持纯 JSON —— 现有调用方一律 `json.load(sys.stdin)`，
提示混入 stdout 会破坏全部既有解析。JSON 内同时带 `hidden_write_tools` 与 `_notice` 字段，人与机器都能取到。

**实测**（2026-09-02）

| 模式 | 输出 |
|---|---|
| demo 只读 | 列出 48 工具 + 提示「已隐藏 30 个写操作工具」并逐一列名 |
| demo `--allow-write` | 「已列出全部 78 个工具（含写操作）」 |
| live 只读 | 「live 账户永久只读：写操作已被代码级拒绝，不存在可用参数解锁的写工具」 |
| live `--allow-write` | `REFUSED`，exit 2，不产生任何网络请求（安全边界未受改动影响） |

---

## 待批准区

_（empty — 提案由 `scripts/review_trade.py` 自动生成后追加到此处；AI 亦可自主起草静置，未经批准不得执行）_

<details>
<summary>已归档的 P003/P004 评审原文（2026-09-02，均已否决，摘要见下方「已否决的提案」）</summary>

### 提案 P003 — 下单接口禁止传 `clOrdId`

- **状态**：`rejected` ❌ 已否决（2026-09-02 用户准其作废）
- **来源**：2026-09-02 10:18 模拟盘接口连通性测试（用户指令的运维验证，非策略交易）
- **是否改动 §4 门槛**：❌ 不改动任何开仓阈值，仅约束下单参数

**实测现象**

```
swap_place_order(..., clOrdId="smoketest_open_01")
→ code "1", message "All operations failed", endpoint POST /api/v5/trade/order

去掉 clOrdId，同一请求原样重发
→ ok, ordId 3886043459539914752, sMsg "Order placed"
```

即：**该 MCP 封装组装 `clOrdId` 时存在问题，传了必然失败，不传必然成功。**
当前自动化流程未使用 `clOrdId`，故不影响现有轮次；但一旦为订单幂等追踪引入该参数，会直接阻断下单。

**提案内容**：在章程 §6（下单执行）中明确 —— **调用 `swap_place_order` / `swap_place_algo_order` 时不得传 `clOrdId`**。
订单追踪改用接口返回的 `ordId`（实测返回完整，如 `3886043459539914752`）。

#### 🔍 运维评审（2026-09-02 · 运维通）

**结论：驳回原文。** 现象真实，但根因定位错误；按原文落地会付出不必要的安全性代价。

**根因复现 —— 对照实验，唯一变量是下划线**

| # | `clOrdId` | 通道 | 结果 |
|---|-----------|------|------|
| 1 | `omt1a` | 降级通道 + `--allow-write` | `sCode "0"` **Order placed**；回显 `clOrdId:"omt1a"`、`ordId: 3886063915865165824` |
| 2 | `omt_1a` | 同上 | `code "1"` **All operations failed** |

两次请求除 ID 中的一个 `_` 外完全相同。OKX 官方规范：`clOrdId` 为**字母与数字组合 / 纯字母 / 纯数字，须以字母开头，区分大小写，最长 32 位**。下划线、连字符均属非法字符（NautilusTrader 的 OKX 适配器文档亦明确记录连字符被拒）。

→ **失败原因是字符非法，不是参数本身。** 原提案把「格式错误」记成了「功能不可用」。

**按原文落地的代价**

禁止传 `clOrdId` 等于**永久放弃订单幂等性**。在交易系统里这是执行层的关键防线：
下单请求超时时，调用方无法区分「交易所未受理」与「已受理但响应丢失」。没有幂等 ID 时只剩两个选择 ——
重试则可能**重复建仓**（敞口翻倍、直接违反 §5 仓位计划），不重试则可能**漏单**。
原提案为规避一个可修复的字符格式问题，牺牲了一道结构性防护。

**修正版（建议替代原文入章程）**

调用 `swap_place_order` / `swap_place_algo_order` 时**应当**传 `clOrdId`，且必须满足：

- 格式 `^[A-Za-z][A-Za-z0-9]{0,31}$` —— 字母开头，仅含字母数字，≤32 字符
- **禁止** `_` `-` 及任何非字母数字字符
- 追踪以接口返回的 `ordId` 为主键，`clOrdId` 用于幂等保护与事后对账

**附带发现**：本次实验同时证实**降级通道可执行写操作**，直接推翻 P004 的前提（见下）。

---

### 提案 P004 — 修订 §2.1：降级通道不支持写操作

- **状态**：`rejected` ❌ 已否决（2026-09-02 用户准其作废）｜ **🔍 运维评审：前提已被证伪 —— 驳回原文；其告警建议保留**
- **来源**：同上，2026-09-02 连通性测试
- **是否改动 §4 门槛**：❌ 不改动

**实测现象**

`scripts/mcp_call.py --profile demo --list-tools` 返回 **48 个工具，全部为只读**，
不含以下任何一个（尽管脚本内 `WRITE_TOOLS` 白名单有定义，但服务端未注册）：

- `swap_place_order` / `swap_place_algo_order`
- `swap_close_position`
- `swap_set_leverage`
- `swap_cancel_order` / `swap_cancel_algo_orders`

**与现行 §2.1 备案的偏差**：§2.1 将 OKX 降级通道描述为可替代 `mcp__okx-demo__*`，
未区分读写。实测结论是：

| 能力 | 能否降级 |
|---|---|
| 行情 / 账户 / 持仓查询 | ✅ 可以 |
| **下单 / 平仓 / 撤单 / 设杠杆** | ❌ **不可以** |

**实质影响**：连接器不可用时，AI **只能做被动持仓管理，无法执行任何交易动作**。
这比 §2.1 备案时的认知严重 —— 不是"换条路走"，而是"可能完全动不了手"。

**提案内容**：修订章程 §2.1，明确标注该通道**仅限只读**；
并补充一条应对规则 —— **当连接器不可用且存在需要平仓 / 补止损的持仓时，
AI 须在当轮邮件中升级为 🔴 高优先级告警**（因为无法执行风控动作，只能告知用户手工处理）。

#### 🔍 运维评审（2026-09-02 · 运维通）

**结论：驳回原文。** 降级通道**支持**写操作，实测下单与撤单均成功。原提案的检测方法本身制造了结论。

**根因定位**

`scripts/mcp_call.py` 的 `spawn()` 默认追加 `--read-only`：

```python
args = [exe, "--profile", profile, "--modules", "all"]
if read_only:
    args.append("--read-only")     # 未加 --allow-write 时必然走到这里
```

上一次「48 个工具全部只读」的观测，**正是在默认只读模式下取得的** —— 先把写工具关掉，再据此断言「服务端未注册写工具」。这是自证式偏差，不是能力缺失。

**加 `--allow-write` 后的实测对比**

| 项 | 默认（只读） | `--allow-write` |
|---|---|---|
| 工具总数 | 48 | **78** |
| `swap_place_order` | ❌ | ✅ |
| `swap_place_algo_order` | ❌ | ✅ |
| `swap_close_position` | ❌ | ✅ |
| `swap_set_leverage` | ❌ | ✅ |
| `swap_cancel_order` | ❌ | ✅ |
| `swap_cancel_algo_orders` | ❌ | ✅ |

**端到端穿透验证**（限价单挂 2300，市价 2406.33，不成交故零手续费，随后撤销）：

- 下单 → `ordId 3886063915865165824`，`sCode "0"` ✅
- 撤单 → `sCode "0"` ✅
- 复核持仓与挂单：均为空 ✅

**安全边界运行时验证**（不只是读代码）：

```
mcp_call.py --profile live --allow-write --tool swap_place_order ...
→ {"ok": false, "error": "REFUSED: live 账户只读（章程 §2），本脚本拒绝一切实盘写操作"}
→ exit code 2，未产生任何网络请求
```

实盘只读强制生效，安全边界真实有效。

**按原文落地的代价（比原缺陷更危险）**

§2.1 现有描述「写操作需显式 `--allow-write`」**本来就是准确的**。
改成「仅限只读」会让 AI 在连接器故障期间**主动放弃唯一可用的应急执行通道** ——
把「能动手」误记为「不能动手」，导致补止损、平仓这些风控动作**该做而不做**。
原缺陷是认知偏差，改正后的章程却会变成行为缺陷。

**真正值得修的（建议替代原文，已立为 P006）**

`--list-tools` 在只读模式下**静默隐藏** 30 个写工具，且不给任何提示 —— 这正是本次误判的直接诱因。脚本应输出提示行，使能力边界可观测。

**予以保留的部分**

「连接器不可用且存在需平仓 / 补止损的持仓时，邮件升级为 🔴 高优先级告警」—— 建议保留，
但**理由改为**：故障期间需明确告知用户系统处于降级状态并已切换执行通道；
而非原文所述「因为无法执行风控动作」（实测证明可以执行）。

</details>

---

## 附录：本文件的区块约定

- **已生效区**（见上方）—— 用户批准后即刻写入章程，同时更新章程 §13 变更记录
- **待批准区**（见上方）—— AI 可自主起草并静置，**未经批准一律不得执行、不得写入章程**
- **已否决的提案**（见下方）—— 保留否决记录，避免 AI 重复提出同一改动

---

## 已否决的提案

### ❌ P003 — 下单接口禁止传 `clOrdId`（2026-09-02 否决）

- **原主张**：`swap_place_order` 不得传 `clOrdId`，追踪改用返回的 `ordId`
- **否决原因**：**现象真实，归因错误。** 根因是 ID 含非法字符，不是参数不可用
- **对照实验**（唯一变量为下划线）：`omt1a` → `sCode "0"` 下单成功；`omt_1a` → `code "1"` All operations failed
- **按原文落地的代价**：等于**永久放弃订单幂等性**。下单超时时无法区分「未受理」与「响应丢失」，
  只能在「重复建仓」与「漏单」间二选一，两者都违反 §5
- **正向替代**：**P005**（启用 `clOrdId` 幂等保护 + 脚本强制格式），已批准入章程 §6.1

> **防复发**：`clOrdId` 格式 `^[A-Za-z][A-Za-z0-9]{0,31}$`，`_` `-` 一律非法。
> 已固化为 `scripts/order_id.py --verify`，手写 ID 前必做自检。

---

### ❌ P004 — 修订 §2.1：降级通道不支持写操作（2026-09-02 否决）

- **原主张**：§2.1 标注降级通道**仅限只读**，连接器故障时邮件升级 🔴 告警（因无法执行风控动作）
- **否决原因**：**前提已被证伪。** 降级通道**支持写**，是默认 `--read-only` 启动造成的观测偏差
- **关键证据**：加 `--allow-write` 后工具数 48 → **78**，下单 `sCode "0"`、撤单 `sCode "0"` 均成功
- **按原文落地的代价**（比原缺陷更危险）：会让 AI 在连接器故障期**主动放弃唯一可用的应急执行通道**，
  导致补止损、平仓等风控动作**该做而不做**。原缺陷是认知偏差，改正后的章程会变成行为缺陷
- **正向替代**：**P006**（提示隐藏写工具，使边界可观测），已批准入章程 §2.1.1
- **保留部分**：🔴 降级状态告警予以保留，但**理由改为**「须告知用户系统处于降级状态」，
  而非原文所述「无法执行风控动作」（实测证明可以执行）

> **防复发（方法论，已写入 `MEMORY.md`）**：观测到「功能不可用 / 数据异常」时，
> 先自查**是不是自己的调用方式（参数、字段、模式）制造了这个现象**，再断言能力缺失或真实亏损。
> 本次 P003（ID 含非法字符）、P004（默认参数关掉写工具）、权益误报（读 `totalEq` 而非 USDT `eq`）
> 三者同源 —— 都是先怀疑外部、后自查内部，顺序反了。
